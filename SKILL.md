---
name: planning-travel-maps
description: 从一个目的地、文字行程或机票酒店预约截图出发，研究景点与美食、比较旅行方案、细化交通和拍照路线，并生成或更新交互式 HTML 旅行地图。支持日期未定、单程或往返机票、有无酒店及已有攻略调整；按需分享发布。
---

# 旅行攻略与交互地图

把用户的旅行想法逐步变成能在现场执行的行程。默认先认识目的地、比较方案，用户选定主方案后再制作地图；已有地图则直接按最新要求更新。用户明确要求一次完成时可直接完成，不增加确认环节。发布仅在用户要求分享链接或部署时执行。

## 从当前阶段继续

- 新行程：先收集事实和偏好，再研究、比较方案。
- 景点咨询：先解释景点和取舍，不强制生成完整行程或 HTML。
- 已选方案：补齐交通、时间和机位细节，再生成地图。
- 修改已有攻略：找到原文件和数据，维护已确认安排，只修改受影响部分。
- 美食选择：读取 [美食研究与选择](references/food-selection.md)，先建立分类候选，再按用户意愿安排到具体日期；不因新增餐厅自动重排景点。
- 分享发布：复用已验证成品和已有站点，读取 [发布指南](references/map-and-publishing.md)。

## 1. 建立行程底稿

先读取 [不同输入的起步方式](references/intake.md)。截图、机票、酒店、确切日期都不是启动条件；文字与截图是同等有效的输入。只有目的地也要先交付有用的初版，不用完整问卷阻塞规划。

提取用户文字和截图中的日期、年份、当地时间、出发到达地点、酒店、活动、餐厅、同行者需求。区分 confirmed（用户确认）、tentative（暂定）、candidate（候选）、unknown（未知）。用户说已订可记为确认；酒店详情页、展示价格、日期选择器本身不是订单证明。模糊文字不能猜成确定事实。

只追问会改变方案的关键缺口，例如出发城市、可用日期、硬性预算或无障碍需求；其余写明假设并继续。机票和活动时间优先于偏好的游玩节奏。保留用户新补充的怕热、拍照、餐饮、避开某场所等偏好，不把京都案例中的喜恶固化为其他旅行的默认值。

节假日必须确认年份、用户实际请假区间和可用天数。中秋、国庆的法定假期、调休、当地假日、客流或价格不能凭记忆推断。多个假期可分别建行程，尚未确认前不拼成一次连续旅行。

保存到项目内 `trip.json`，格式见 [数据约定](references/trip-data.md)。阶段未到时不要求填满所有字段。避免重复询问已有答案，不将姓名、订单号、证件号复制到分享成品。

## 2. 先认识目的地，再比较方案

按地理片区介绍有代表性的景点，用易理解的文字解释“看什么、为什么值得、谁适合”。景点认识卡包含代表景观、停留时间、步行与台阶情况、遮阴或室内条件、适合时段、顺路搭配，配真实图片或可靠入口。

真实图片需标注来源并检查展示是否成功；失败后改用可访问来源或直接链接。不能用生成图冒充景点实拍，不能反复交付同一种失效图片。

检索会变化的信息，以官方景区、活动、交通运营商、餐厅为优先来源。记录来源和核验日期，无法核实的标成待确认。细则见 [研究与路线](references/research-and-routing.md)。

通常给两到三套取舍明确的方案，例如经典必看、街景拍照、轻松慢游。比较覆盖景点、强度、换酒店次数、交通成本和放弃项，不只改名称。检查首次到访的代表景点是否遗漏；主动说明为何舍弃，不把所有热门景点强塞进去。

推荐一个主方案，邀请用户选择或修改。若用户已经选择，就继续执行，不再次索要相同确认。

## 3. 细化成可执行路线

以酒店或实际住宿点为出发和返回锚点。逐日列出出发时间、停留区间、交通段、缓冲、用餐和休息，区分估算与固定预约。

对关键段写清：出租车上下车参考点、步行入口、车站全名、线路和方向、必要换乘。仅在核验后写具体出口或站台；车辆可达位置、临时管制和末班车要另核查。

拍照行程增加局部编号，如清1、坂1、稻1；解释代表画面、拍摄方向或参照物、建议时间和优先级。给出“必拍、顺路、可删”层次，不承诺无人空景。景区内按实际道路和出入口安排，而非坐标直线距离。

提供与行程相关的降级方案：起晚、炎热、降雨、排队或临时闭馆时删什么、保什么。固定活动日把到场和返程放在首位，算上入场、散场、换装、行李与预约归还时间。

## 4. 生成或更新 HTML 地图

餐饮是可独立推进的阶段。用户要求先定景点、后定吃的时，保留该节奏；开始研究美食时按品类、位置、评分证据与适配程度组织候选，遵循 [美食研究与选择](references/food-selection.md)，不要用单一评分榜代替推荐。

读取 [地图与发布指南](references/map-and-publishing.md)。以同一份 `trip.json` 驱动行程文字、编号、点位和导航链接，防止多份手工数据互相矛盾。

默认交付：`trip.json`、简明 `itinerary.md`、可本地打开的 `trip-map.html`。沿用已有文件名和项目架构，避免为单文件旅行地图新建无关框架。地图应有全程、单日、分段视图，点位详情、图层、外部导航和移动端布局。

生成的 HTML 默认支持中文与英文切换，读取 [双语输出](references/localization.md)。这不仅是翻译按钮：行程、景点、美食、弹窗、筛选、提示和无障碍标签均须覆盖。修改已有单语地图时补齐双语数据与渲染，保持同一套点位和路线，不复制两套易失配的地图。

路线顺序示意和真实道路导航必须明确区分。没有真实路线数据时，线条只表达顺序，不给“沿线走”的暗示；提供逐段导航入口及景区官方境内图。地图外部资源失效时，文字行程仍可阅读。不要称依赖在线瓦片的 HTML 为完全离线地图。

## 5. 验证、交付与迭代

交付前检查：日期时区、预约冲突、交通缓冲、遗漏景点、所有点位和路线引用、地图与文字一致性、手机图层和编号、图片与外链。针对本次新增或改变的功能验证，记录未完成检查和原因。

提供本地文件和可用预览入口。需要服务时先确认文件目录和端口；`localhost` 只能在本机访问，不能当作分享链接。

用户要求发布时按发布指南执行，验证线上是同一最新版后给链接。未成功发布不能声称成功；不能承诺永久访问。

用户反馈后修正数据和受影响的地图、文字、导航，再复查。只有用户明确要求调整 skill 时才编辑 skill 本身，避免把一次行程偏好写成永久规则。回归试用可使用 [测试场景](references/trial-scenarios.md)。
